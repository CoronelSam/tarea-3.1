import 'package:flutter/material.dart';

class TabCalls extends StatelessWidget {
  const TabCalls({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.call);
  }
}